Steps to run the app:

1. Execute the statements in create_db.sql
2. Run the maven target - "mvn spring-boot:run"

The initial delay and the interval can be changed in src\main\resources\application.properties